{
  "name": "lagah-crash-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute lagah-crash-bot-db --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute lagah-crash-bot-db --file=./schema.sql --remote",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
